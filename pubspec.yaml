name: api_service
description: API Service Package.

version: 1.2.0
publish_to: "none"

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  dartz: 0.10.1
  dio: 5.8.0+1
  flutter:
    sdk: flutter
  freezed_annotation: 3.0.0

dev_dependencies:
  build_runner: 2.4.15
  flutter_test:
    sdk: flutter
  freezed: 3.0.6
  lint: 2.8.0
